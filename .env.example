# Copy this file to .env and fill in real values. .env is gitignored —
# never commit real keys.

# LLM calls (planner, synthesizer, OCR vision fallback) — openrouter.ai
OPENROUTER_API_KEY=

# Embeddings for the semantic_search fallback tool — voyageai.com
VOYAGE_API_KEY=

# Stage 2 server
PORT=8080
